version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -f
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/xped8
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
